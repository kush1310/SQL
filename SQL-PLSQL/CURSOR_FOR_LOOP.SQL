-- PROGRAM FOR CURSOR USING FOR LOOP
DECLARE
	CURSOR C_2 IS SELECT CID, CNAME FROM CUSTOMER;
BEGIN
	-- OPEN C_2;
	FOR I IN C_2
	LOOP
		DBMS_OUTPUT.PUT_LINE(I.CID || ' ' || I.CNAME);
	END LOOP;
	-- CLOSE C_2;
END;
/